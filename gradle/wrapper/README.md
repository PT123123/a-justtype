# Gradle Wrapper

The gradle-wrapper.jar binary is required for the Gradle wrapper to work.
It can be generated by running the wrapper task from a system Gradle installation:

```bash
gradle wrapper --gradle-version 8.9
```

Or download from:
https://services.gradle.org/distributions/gradle-8.9-bin.zip
